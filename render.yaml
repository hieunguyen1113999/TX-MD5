services:
  - type: web
    name: tx-md5-predict
    runtime: python
    plan: free
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --preload app_tx_md5_upgraded:app
